import React from 'react'

function AccountSettings() {
  // This page is no longer needed - BusinessProfileCard opens from Layout
  // Keeping this component for backward compatibility with routes
  return null;
}

export default AccountSettings
